import Worker from './adapter'
/**
 * Creates REST API interface.
 *
 * @function
 * @return {Object} REST API interface.
 */
const createWorkerApi = () => {
  return Worker
}

export { createWorkerApi }
